# Development environment - uses a separate Google OAuth app so dev/test data
# is completely isolated from production wealth data (separate Drive AppData).
VITE_GOOGLE_CLIENT_ID=DEV_CLIENT_ID_PLACEHOLDER.apps.googleusercontent.com
VITE_ENV_LABEL=DEVELOPMENT
